#include <stdio.h>
#include<string.h>
int main() {
    
char str[50],s[50];
scanf("%s",str);
int n,i,j=0;
n=strlen(str);
for(i=n-1;i>=0;i--){
    s[j]=str[i];
    j++;
}
    s[j]='\0';
    printf("%s",s);


 
 

return 0;
}
