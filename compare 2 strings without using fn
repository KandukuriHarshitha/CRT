#include <stdio.h>
#include<string.h>
int main() {
    
char str[50],s[50];
scanf("%s",str);
scanf("%s",s);
int n1,n2;
for(n1=0;str[n1]!='\0';n1++);
for(n2=0;s[n2]!='\0';n2++);
if(n1==n2){
    printf("Same");
}
else{
    printf("Not same");
}
 
 

return 0;
}
